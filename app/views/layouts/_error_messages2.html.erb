<% if @user.errors.any? %>
  <div class="alert alert-warning">
    <ul>
      <li class="error-message"><%= @user.errors.count %>
<%= "error" %></li>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>