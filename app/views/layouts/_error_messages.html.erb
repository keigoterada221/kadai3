<% if @book.errors.any? %>
  <div class="alert alert-warning">
    <ul>
      <li class="error-message"><%= @book.errors.count %>
<%= "error" %></li>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>