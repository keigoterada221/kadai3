<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>
<div class="book-edit">
<h1>Editing Book</h1>

<%= form_for(@book) do |f| %>
<%= render 'layouts/error_messages', model: f.object %>
<!-- render入れる -->
<h4>Title</h4>
<%= f.text_field :title,class: "edit-form" %>

<h4>Opinion</h4>
<%= f.text_area :body,class: "edit-form" %><br>

<button class="btn btn-primary book-edit-btn"><%= f.submit "Update Book",class: "btn btn-primary"%></button><br>

<%= link_to "Show" ,book_path %>
<%="|"%>
<%= link_to "Back" ,books_path %>

<% end %>
</div>
</body>
</html>